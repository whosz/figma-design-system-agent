Read and follow `AGENTS.md` in the repository root — it is the single
source of truth for this repository's rules, skill catalog, dependency
graph and pipelines.

Before any task, locate the matching skill in `skills/*/SKILL.md` and
follow its instructions. Slash-prompts in `.github/prompts/` are thin
wrappers over those skills.
