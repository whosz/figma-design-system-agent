Read and follow `AGENTS.md` — it is the single source of truth for this
repository's rules, skill catalog, dependency graph and pipelines.
Skills are in `skills/*/SKILL.md`.

Note: this repo ships `.mcp.json` configured for the LOCAL Figma MCP server
(Figma desktop app required). For the remote server, change the url to
https://mcp.figma.com/mcp and re-authenticate.
